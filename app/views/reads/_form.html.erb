<%= form_for(read) do |f| %>
  <% if read.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(read.errors.count, "error") %> prohibited this read from being saved:</h2>

      <ul>
      <% read.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id %>
    <%= f.select :person_id, options_from_collection_for_select(Person.order(:name).all, "id", "name") %>
  </div>

  <div class="field">
    <%= f.label :book_id %>
    <%= f.select :book_id, options_from_collection_for_select(Book.order(:author).all, "id", "author") %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
